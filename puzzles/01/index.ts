import { PuzzleSolver, Stars } from '../../advent-of-code.types';

const solveFirstPuzzle: PuzzleSolver = () => (
	'placeholder first puzzle solution'
);

const solveSecondPuzzle: PuzzleSolver = () => (
	'placeholder second puzzle solution'
);

const stars: Stars = {
	'1': solveFirstPuzzle,
	'2': solveSecondPuzzle,
};

export default stars;